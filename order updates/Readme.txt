** all the dublicate files that are named copy are the older files, may be used when a reverse is needed.*** 

- header section with links remved

- Sync function disabled (funtion name ->sync_api)

- Get updates funtion disabled (funtion name ->get_updates)